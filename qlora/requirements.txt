# HuggingFace stack for the QLoRA engine. torch comes from the base image.
transformers>=4.45
peft>=0.13
accelerate>=1.0
datasets>=3.0
bitsandbytes>=0.45   # 4-bit QLoRA for 7B+ (needs a Blackwell/sm_120-capable build)
