# HuggingFace stack for the QLoRA engine. torch comes from the base image.
transformers>=4.45
peft>=0.13
accelerate>=1.0
datasets>=3.0
# only needed for --four-bit (7B+); 1.5B in bf16 does not need it:
# bitsandbytes>=0.44
